This Petri Nets Simulator application is developed in C# language.

To run and test the app, you must have:
	1. IDE: Visual Studio 2015+
	2. Define the  input file location (JSONParser class entry-to-path (JSON format)). 
		An example of a JSON file with which the application was tested is found in the root directory.
